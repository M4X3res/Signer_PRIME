Уточнение по предыдущему промпту (PROMPT_FIX_VECTOR_TILE_BROKEN_RENDER.md):

Корректировка по токену: символ * в значении параметра token= — это не ошибка и не два склеенных токена, а часть валидного формата токена, который выдаёт api.maps.by. Убери из промпта и из кода все проверки/предупреждения, которые трактуют * в токене как признак "битого/дублированного токена" — эта эвристика была неверной для данного провайдера и не должна блокировать сохранение URL.

Единственное, что реально нужно поправить в самом URL — это висящий & в конце строки (после значения токена), который не имеет смысла, если после него нет следующего параметра. Нужно:

В ui/widgets/settings_page.py (_check_tile_url_mismatch()) убрать/переработать проверку на * в токене как "подозрительный паттерн" — оставить только проверку на:
URL, обрывающийся на &, ? или = без последующего параметра (это реальная ошибка, её нужно продолжать ловить);
дублирование token= (несколько вхождений подряд — это по-прежнему валидный признак ошибки, но НЕ путай с одним токеном, который просто содержит * как часть значения);
пробелы, && — оставить как есть.
Обнови docstring/комментарии в _check_tile_url_mismatch(), чтобы явно отметить: символ * внутри значения token= сам по себе НЕ является признаком ошибки для api.maps.by и подобных провайдеров, и не должен триггерить предупреждение.
Основная (реальная) проблема из бага остаётся прежней и её фикс не меняется: URL должен содержать шаблонные плейсхолдеры {z}/{x}/{y} вместо конкретных чисел координат тайла — это и была причина "мозаики" на скриншоте. Эту часть промпта оставь без изменений.
Обнови существующие тесты, которые проверяли детекцию * как ошибки — убери или инвертируй соответствующие assert'ы.