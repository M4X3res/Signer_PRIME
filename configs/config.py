"""
configs/config.py
Глобальное состояние обработки.
Изменяется только из ProcessingController и VideoReaderThread.
UI читает через сигналы — не напрямую.
"""

# ── Пути ──────────────────────────────────────────────────────────
PATH_TO_VIDEO:        str = ""
PATH_TO_EXTRA_LAYERS: str = ""
PATH_TO_GEOJSON:      str = ""
PATH_TO_GPX:          str = ""

# ── Список видеофайлов ────────────────────────────────────────────
VIDEOS: list[str] = []

# ── Метаданные видео ──────────────────────────────────────────────
FRAME_WIDTH:  int = 1920   # ширина кадра (px), обновляется при открытии видео
FRAME_HEIGHT: int = 1080   # высота кадра (px), обновляется при открытии видео
FRAMES_PER_VIDEO: int = 63600  # Кадров в одном видеофайле (60fps * 60s * 17.67min)
VIDEO_FPS: float = 60.0    # BLOCK J.2: Реальный FPS видео, обновляется при открытии

# Task C: Кэш реальных длин видеофайлов (заполняется лениво через cv2)
VIDEO_FRAME_COUNTS: list[int] = []

# ── Состояние обработки ───────────────────────────────────────────
FRAME_STEP:             int = 5     # каждый N-й кадр обрабатывается
COUNT_PROCESSED_FRAMES: int = 0

INDEX_OF_FRAME:    int = 0   # текущий кадр внутри видеофайла
INDEX_OF_VIDEO:    int = 0   # индекс текущего видеофайла
INDEX_OF_All_FRAME:int = 0   # глобальный номер кадра
INDEX_OF_GPS:      int = 0   # индекс GPS точки
INDEX_OF_SING:     int = 0   # индекс знака (устаревшее, для совместимости)

# Примечание: эти значения автоматически сбрасываются при старте новой обработки

COUNT_FRAMES:      int = 0   # общее число кадров

# ── GPS / видео время ─────────────────────────────────────────────
SECONDS_ALL_VIDEO: float = 0.0  # текущая секунда по всем видео

# ── Прочее ───────────────────────────────────────────────────────
CLASSIFIER: dict = {}
FEATURES:   list = []


# ── Профилирование производительности ─────────────────────────────
ENABLE_PROFILING: bool = False  # Включить детальное профилирование
PROFILING_MIN_FRAMES: int = 1000  # Минимум кадров для статистики
PROFILING_REPORT_INTERVAL: int = 500  # Печатать отчет каждые N кадров

# ── Многопоточность ───────────────────────────────────────────────
# Режим обработки: "single_thread", "process_pool", "gpu_batch"
PROCESSING_MODE: str = "single_thread"

# Количество воркеров для параллельной обработки
# None = автоопределение (cpu_count - 1)
N_WORKERS: int | None = None

# Максимальный размер очереди задач для воркеров
WORKER_QUEUE_SIZE: int = 16

# Максимальный "забег вперед" reorder buffer (кадров)
REORDER_BUFFER_MAX_GAP: int = 32

# BLOCK FIX-2.1: реальный интервал (в кадрах) между двумя ОБРАБОТАННЫМИ кадрами,
# = FRAME_STEP * текущий адаптивный skip DetectorThread. Обновляется из
# DetectorThread на каждой итерации цикла. SignHandler использует это значение,
# чтобы адаптировать пороги давности трека к реальному интервалу между
# кадрами, а не к жёстко зашитым числам, рассчитанным на FRAME_STEP=5, skip=1.
CURRENT_EFFECTIVE_SKIP: int = 1
