# ═══════════════════════════════════════════════════════════════════
# RoadScanner (Signer PRIME) — основные зависимости для запуска
# ═══════════════════════════════════════════════════════════════════

# ── GUI ───────────────────────────────────────────────────────────
PyQt6>=6.6.0
PyQt6-WebEngine>=6.6.0

# ── CV / ML ───────────────────────────────────────────────────────
opencv-python>=4.9.0
ultralytics>=8.2.0
easyocr>=1.7.1
joblib>=1.4.0
numpy>=1.26.0
Pillow>=10.3.0
# torch и torchvision устанавливаются автоматически через ultralytics

# ── GPS / Geo ─────────────────────────────────────────────────────
gpxpy>=1.6.2
geopy>=2.4.1
geojson>=3.1.0
pyproj>=3.6.1
requests>=2.31.0
shapely>=2.0.0  # Опционально, улучшает точность геометрии пересечений

# ── Licensing ─────────────────────────────────────────────────────
cryptography>=41.0.0  # Ed25519 для проверки подписи токенов лицензии

# ── Server (для карты) ────────────────────────────────────────────
flask>=3.0.3
flask-cors>=4.0.1
flask-socketio>=5.3.6
python-engineio>=4.9.0
python-socketio>=5.11.0
jinja2>=3.1.4