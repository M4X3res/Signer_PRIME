# ── GUI ───────────────────────────────────────────────────────────
PyQt6>=6.6.0
PyQt6-WebEngine>=6.6.0

# ── CV / ML ───────────────────────────────────────────────────────
opencv-python>=4.9.0
ultralytics>=8.2.0
easyocr>=1.7.1
joblib>=1.4.0
numpy>=1.26.0
Pillow>=10.3.0

# ── ONNX / OpenVINO (BLOCK M: CPU-инференс бэкенды) ──────────────
onnx>=1.22.0
onnxruntime>=1.29.0  # CPU-only версия (НЕ onnxruntime-gpu!)
openvino>=2024.0  # BLOCK FIX-2.1: разкомментировано, теперь обязательная зависимость
openvino-dev>=2024.0  # Необходим для экспорта в OpenVINO формат

# ── GPS / Geo ─────────────────────────────────────────────────────
gpxpy>=1.6.2
geopy>=2.4.1
geojson>=3.1.0
pyproj>=3.6.1
requests>=2.31.0

# ── Server ────────────────────────────────────────────────────────
flask>=3.0.3
flask-cors>=4.0.1
flask-socketio>=5.3.6
python-engineio>=4.9.0
python-socketio>=5.11.0
jinja2>=3.1.4

# ── Utils ─────────────────────────────────────────────────────────
# (неиспользуемые зависимости удалены: pandas, matplotlib)