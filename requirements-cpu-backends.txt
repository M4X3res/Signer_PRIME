# ═══════════════════════════════════════════════════════════════════
# RoadScanner (Signer PRIME) — опциональные CPU-инференс бэкенды
# ═══════════════════════════════════════════════════════════════════
#
# По умолчанию приложение использует PyTorch (torch) для инференса.
# Эти зависимости опциональны и нужны только для альтернативных
# бэкендов CPU-инференса (ONNX Runtime, OpenVINO).
#
# Установка:
#   pip install -r requirements-cpu-backends.txt
#
# Примечание: 
# - НЕ устанавливайте onnxruntime-gpu, он конфликтует с CPU-версией!
# - openvino-dev нужен только для экспорта моделей в OpenVINO формат,
#   для использования готовых OpenVINO-моделей достаточно openvino.
# - Версии зафиксированы точно (==) для обеспечения совместимости
#   между экспортом и runtime.
# ═══════════════════════════════════════════════════════════════════

onnx==1.17.0
onnxruntime==1.19.2  # CPU-only (НЕ onnxruntime-gpu!)
coloredlogs>=15.0.1  # Required by onnxruntime
openvino==2024.4.0
openvino-dev==2024.4.0  # Для экспорта моделей (scripts/export_models_onnx.py)
