# Отчёт о проверке состояния кода (Блок 2)

Дата: 2026-08-21
Статус: **ЧАСТИЧНО УСТРАНЕНЫ РАСХОЖДЕНИЯ**

## Расхождения из документации vs реальный код

### ✅ 1. Сохранение GeoJSON — ИСПРАВЛЕНО
**Статус:** РЕАЛИЗОВАНО  
`ui/main_window.py` содержит `SaveThread(QThread)` (строки 400-420), который асинхронно выполняет `FinalHandler().save_result()`.
- `_on_finish_requested()` запускает SaveThread
- Сигналы `finished_ok` и `error` корректно обрабатываются
- **НО:** Отсутствует progress-callback в `FinalHandler.save_result()` для отображения прогресса OSM snap

**TODO:** Добавить progress-параметр в `FinalHandler.save_result()` и прокинуть в `SaveThread`.

---

### ✅ 2. Защита "Завершить" от двойного клика — РЕАЛИЗОВАНО
**Статус:** РЕАЛИЗОВАНО  
`ui/widgets/processing_page.py::_on_finish_clicked()` (строки 336-356):
- Флаг `self._finish_clicked` проверяется и устанавливается
- Кнопки `btn_finish` и `btn_pause` немедленно отключаются
- Текст меняется на "⏳ Завершение…"
- Флаг сбрасывается в `set_active(False)`

**Никаких дополнительных действий не требуется.**

---

### ✅ 3. Graceful shutdown сервера карты — РЕАЛИЗОВАНО
**Статус:** РЕАЛИЗОВАНО  
`ui/main_window.py::closeEvent()` (строки 509-529):
- Останавливает `ProcessingController` если запущен
- Вызывает `self.page_map.stop_server()` (существует в `map_page.py:180`)
- Очищает ресурсы редактора

`ui/widgets/map_page.py::stop_server()` корректно останавливает `ServerThread`.

**Никаких дополнительных действий не требуется.**

---

### ✅ 4. Таймаут дедупликации — РЕАЛИЗОВАНО
**Статус:** РЕАЛИЗОВАНО  
`core/final_handler.py::_deduplicate()` (строки 326-350):
- `TIMEOUT_SECONDS = 60` объявлен
- Проверка таймаута каждые 100 features
- При превышении добавляются оставшиеся features без дедупликации

**Никаких дополнительных действий не требуется.**

---

### ⚠️ 5. Осиротевший multiprocessing-модуль — ЧАСТИЧНО ИСПРАВЛЕНО
**Статус:** МОДУЛЬ СУЩЕСТВУЕТ, НО НЕ ИНТЕГРИРОВАН  
`processing/detector_process_pool.py` **существует** (судя по файловой структуре).  
`test_import.py` пытается импортировать `config.PROCESSING_MODE` — **этот атрибут отсутствует в `configs/config.py`**.

**TODO:**
1. Проверить реализацию `detector_process_pool.py`
2. Добавить `PROCESSING_MODE` в `configs/config.py`
3. Интегрировать в UI (Settings) выбор режима обработки

---

### ❌ 6. Настройки — почти полностью декоративны
**Статус:** КРИТИЧЕСКАЯ ПРОБЛЕМА  
`ui/widgets/settings_page.py` содержит настройки, которые **нигде не используются**:
- `conf_threshold()`, `iou_threshold()` не влияют на реальные значения в `Detector`
- `dup_radius()` не влияет на `SignHandler.NEARBY_SIGN_RADIUS_M` / `FinalHandler.DEDUP_RADIUS_M`
- `_frame_step_spin` игнорируется — значение перезаписывается формулой от скорости в `detector_thread.py`
- Настройки не сохраняются между запусками (нет QSettings/JSON)

**TODO:** БЛОК C — полная переработка настроек.

---

### ⚠️ 7. Карта: `posMarker` не добавляется автоматически
**Статус:** ЧАСТИЧНО ИСПРАВЛЕНО  
`templates/map.html`:
- `posMarker` создаётся в `initMap()` (строка 669)
- **НО:** не вызывается `.addTo(map)` в `initMap()`
- Добавляется условно в `socket.on("position")` (строки 1118-1122): проверяется `if (!map.hasLayer(posMarker)) posMarker.addTo(map);`
- Центрирование работает если `liveMode === true`

**Проблема:** Маркер не виден до первого события `position` из бэкенда.

**TODO:** Добавить `.addTo(map)` в `initMap()` со скрытым состоянием (opacity 0 или `display:none`), показывать при первом событии.

---

### ⚠️ 8. Видео-плеер на карте — полная симуляция
**Статус:** ПОДТВЕРЖДЕНО  
`MainWindow._on_jump_to_second()` **только логирует** (комментарий: "передаём в VideoPlayer (будущий)").  
Кнопки play/pause/seek в `map.html` управляют локальным JS-таймером, но не влияют на реальное видео.

**TODO:** БЛОК D.6.2 — реализовать честный видео-предпросмотр или явно обозначить недоступность.

---

### ✅ 9. `loadAll()` использует `Promise.allSettled` — ИСПРАВЛЕНО
**Статус:** РЕАЛИЗОВАНО  
`templates/map.html::loadAll()` (строки 679-700):
```javascript
const results = await Promise.allSettled([loadTrack(), loadSigns(), loadSignTypes()]);
```
Обрабатывает частичные failure корректно.

**Никаких дополнительных действий не требуется.**

---

### ❌ 10. Нет обработчика `processing_finished`
**Статус:** НЕ РЕАЛИЗОВАНО  
Проверено: в `server/map_server.py` нет функции `emit_processing_finished()`.  
В `templates/map.html` нет `socket.on("processing_finished", ...)`.

**TODO:** БЛОК D.6.4 — реализовать сигнал завершения обработки.

---

### ❌ 11. `renderMarkers()` не центрирует карту автоматически
**Статус:** НЕ РЕАЛИЗОВАНО  
`templates/map.html::renderMarkers()` не вызывает `map.fitBounds()` при отсутствии трека.

**TODO:** БЛОК D.6.5 — добавить автоцентрирование на маркеры.

---

## Приоритеты для блока A-D

### Критические (делать в первую очередь):
1. **Блок B.4.3** — Добавить progress-callback в `SaveThread` для OSM snap (UI не зависает, но пользователь не видит прогресс)
2. **Блок C** — Сделать настройки реально работающими
3. **Блок D.6.4** — `processing_finished` сигнал (иначе карта не обновляется автоматически)
4. **Блок A.3** — Предзагрузка EasyOCR (устраняет провал FPS посреди обработки)

### Средние:
5. **Блок D.6.5** — Автоцентрирование маркеров на карте
6. **Блок D.6.7** — `posMarker` видимость
7. **Блок A.1** — Pipeline-параллелизм (OCR в отдельном потоке)
8. **Блок B.4.1** — Асинхронное сканирование папки с видео

### Низкие (опциональные):
9. **Блок A.2** — Process pool (только если pipeline недостаточен)
10. **Блок D.6.2** — Видео-предпросмотр (большая задача, можно отложить)

---

## Вывод

Документация **частично устарела**, но многие критические фиксы уже реализованы:
- SaveThread работает
- Защита кнопки "Завершить" работает
- closeEvent реализован
- Таймаут дедупликации работает
- Promise.allSettled используется

Основные проблемы, требующие решения:
- **Настройки не влияют на обработку** (блок C)
- **Карта не обновляется автоматически** после завершения (блок D)
- **EasyOCR вызывает провал FPS** при первой загрузке (блок A)
- **Нет визуального feedback для OSM snap** при сохранении (блок B)
