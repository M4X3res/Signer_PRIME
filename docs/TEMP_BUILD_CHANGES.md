# ⚠️ ВРЕМЕННЫЕ ИЗМЕНЕНИЯ ДЛЯ ТЕСТОВОГО БИЛДА

## Что было изменено

### 1. `licensing/public_key.py`
- Добавлена переменная окружения `SKIP_PROD_KEY_CHECK=1`
- Позволяет собрать .exe с dev-ключом для тестирования

### 2. `scripts/build/prepare_release.bat`
- Установлен `set SKIP_PROD_KEY_CHECK=1`
- Пропускает проверку production ключа

## ⚠️ ВАЖНО

**ЭТИ ИЗМЕНЕНИЯ ТОЛЬКО ДЛЯ ТЕСТИРОВАНИЯ!**

**ПЕРЕД PRODUCTION РЕЛИЗОМ ОБЯЗАТЕЛЬНО:**

1. **Удалить `set SKIP_PROD_KEY_CHECK=1` из prepare_release.bat**

2. **Сгенерировать production ключи:**
   ```bash
   python scripts/generate_ed25519_keys.py
   ```

3. **Обновить публичный ключ в `licensing/public_key.py`**

4. **Обновить приватный ключ на сервере** (в .env файле)

5. **Удалить проверку `SKIP_PROD_KEY_CHECK` из `licensing/public_key.py`** 
   Или хотя бы убедиться что она НЕ установлена при билде

## Коммиты для отката

```bash
# Текущий коммит с временным решением
git log --oneline -1
# a12a6c5 temp: skip production key check for testing build

# Для отката:
git revert a12a6c5
```

## Что будет работать сейчас

✅ Сборка .exe пройдёт успешно  
✅ Приложение запустится с локальным сервером  
⚠️ Использует dev-ключ (НЕБЕЗОПАСНО для production!)

## Что НЕ будет работать

❌ Лицензии подписанные production ключом не будут проверяться  
❌ НЕ безопасно для реальных пользователей

## Дата создания

2026-09-14 17:40
